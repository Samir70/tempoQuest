<%= form_with(model: practise_style) do |form| %>
  <% if practise_style.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(practise_style.errors.count, "error") %> prohibited this practise_style from being saved:</h2>

      <ul>
        <% practise_style.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
